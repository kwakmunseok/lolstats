RIOT_API_KEY=
DB_USERNAME=lolstats
DB_PASSWORD=lolstats
# HS256 key - any string works, it's SHA-256-hashed to 32 bytes before signing (JwtTokenService),
# but use a long random value in prod (e.g. `openssl rand -base64 32`).
JWT_SECRET=
