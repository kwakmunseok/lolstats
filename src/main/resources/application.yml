spring:
  application:
    name: lolstats
  profiles:
    active: dev
  jackson:
    deserialization:
      fail-on-unknown-properties: false
  mvc:
    problemdetails:
      enabled: true

ddragon:
  base-url: https://ddragon.leagueoflegends.com
  refresh-interval-hours: 24
